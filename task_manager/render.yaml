services:
  - type: web
    name: task-manager
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn task_manager.app:app
    healthCheckPath: /healthcheck
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: SECRET_KEY
        value: REPLACE_ME
      - key: DATABASE_URL
        value: REPLACE_ME